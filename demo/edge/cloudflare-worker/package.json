{
  "name": "poli-page-sdk-edge-demo",
  "version": "0.0.0",
  "description": "Cloudflare Workers demo for @poli-page/sdk — proves the main entry is isomorphic.",
  "private": true,
  "scripts": {
    "dev": "node dev.mjs",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@poli-page/sdk": "file:../../.."
  },
  "devDependencies": {
    "wrangler": "^4.86.0"
  }
}
